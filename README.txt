# Veryss Static Site
This folder contains a ready-to-deploy static site.

Deploy options:
- **Netlify**: drag the folder into the dashboard.
- **Vercel**: `vercel` from this folder.
- **GitHub Pages**: push to a repo and enable Pages (root, main branch).
- **Any hosting**: upload `index.html` and the `images/` folder.
